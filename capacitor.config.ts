import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.glowbreeze.healthlog',
  appName: 'Kid Health Log',
  webDir: 'dist/client'
};

export default config;
